# 图书管理系统需求文档

## 1. 项目概述

### 1.1 项目背景

本项目为 Java 课程设计作业，目标是构建一个具备良好交互体验、数据持久化能力、且贴近实际生产实践的图书管理系统。

### 1.2 项目目标

实现一个基于 Java 的桌面图书管理系统，支持图书销售、图书信息管理、多条件查询、统计分析以及数据持久化，并通过图形化界面提升易用性。

### 1.3 适用范围

适用于单机环境下的图书销售与库存管理场景，可作为课程设计演示及后续扩展的基础版本。

---

## 2. 功能需求

### 2.1 图书销售

- 用户输入 ISBN 号，系统查询图书信息。
- 若图书存在且有库存，用户输入购买数量。
- 系统计算应收金额，扣减库存，并给出销售成功提示。
- 若库存不足，系统提示库存不足并结束本次销售。
- 应收金额计算精确到分，使用 `BigDecimal` 避免浮点误差。

### 2.2 图书信息管理

#### 2.2.1 添加图书

- 录入书名、出版社、ISBN、作者、库存量、价格。
- ISBN 号全局唯一，重复时提示并取消添加。
- 价格和库存量必须为非负数，必填项不能为空。

#### 2.2.2 查询图书

- 支持按书名、ISBN、作者、出版社进行查询。
- 书名、作者、出版社支持模糊查询。
- 查询结果在表格中展示。
- 无匹配记录时提示“该标题不存在！”。

#### 2.2.3 修改图书

- 根据查询结果选择记录进行修改。
- 修改时保持 ISBN 唯一性约束。
- 修改后刷新图书列表。

#### 2.2.4 删除图书

- 输入或选择要删除的 ISBN。
- 删除前进行二次确认。
- ISBN 不存在时提示“该编号不存在”。

### 2.3 统计功能

- 输出当前书库中图书总数及详细信息。
- 支持按价格、库存量、作者、出版社进行统计。
- 统计结果按指定字段从大到小排序展示。
- 支持排序字段切换及升序/降序调整。

### 2.4 数据持久化

- 使用 H2 嵌入式数据库存储图书数据。
- 程序启动时自动初始化数据库表结构。
- 程序运行期间的所有增删改查操作实时反映到数据库。
- 程序重启后数据不丢失。

---

## 3. 非功能需求

### 3.1 性能需求

- 单次查询响应时间不超过 1 秒（适用于万级以下图书数据）。
- 启动时间不超过 5 秒。

### 3.2 可靠性需求

- 数据库连接异常时给出友好提示，程序不崩溃。
- 输入非法数据时进行校验并给出明确错误信息。

### 3.3 易用性需求

- 采用图形化界面（Swing），降低操作门槛。
- 所有关键操作均有即时反馈。
- 表格支持排序、选中、滚动浏览。

### 3.4 可维护性需求

- 采用三层架构，表现层、业务逻辑层、数据访问层分离。
- 代码结构清晰，命名规范，关键逻辑添加注释。
- 使用 Maven 管理依赖，便于后续扩展。

---

## 4. 技术选型

| 技术/工具 | 选型 | 说明 |
|----------|------|------|
| 编程语言 | Java 17+ | 现代 Java 特性支持 |
| 构建工具 | Maven | 依赖管理与项目构建 |
| GUI 框架 | Swing | JDK 内置，无需额外运行环境 |
| 数据库 | H2 Database | 嵌入式数据库，零安装，随程序启动 |
| 数据库访问 | JDBC | 原生 SQL，便于学习和展示 |
| 金额计算 | BigDecimal | 避免浮点数精度问题 |

---

## 5. 系统架构

系统采用经典的三层架构：

```
┌─────────────────────────────────────┐
│  表现层（Presentation Layer）        │
│  Swing GUI：主窗口、图书管理面板、     │
│  销售面板、统计面板                   │
├─────────────────────────────────────┤
│  业务逻辑层（Service Layer）         │
│  BookService：销售、查询、修改、删除、 │
│  统计排序等业务规则                   │
├─────────────────────────────────────┤
│  数据访问层（Data Access Layer）     │
│  BookRepository / JdbcBookRepository │
│  负责 H2 数据库连接与 CRUD 操作       │
├─────────────────────────────────────┤
│  实体层（Entity Layer）              │
│  Book：图书数据模型                   │
└─────────────────────────────────────┘
```

---

## 6. 数据库设计

### 6.1 图书表（books）

```sql
CREATE TABLE IF NOT EXISTS books (
    isbn VARCHAR(20) PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    publisher VARCHAR(100),
    author VARCHAR(100),
    stock INT NOT NULL CHECK (stock >= 0),
    price DECIMAL(10, 2) NOT NULL CHECK (price >= 0)
);
```

### 6.2 字段说明

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| isbn | VARCHAR(20) | PRIMARY KEY | 国际标准书号，唯一标识 |
| title | VARCHAR(100) | NOT NULL | 书名 |
| publisher | VARCHAR(100) | | 出版社 |
| author | VARCHAR(100) | | 作者 |
| stock | INT | NOT NULL, CHECK >= 0 | 库存量 |
| price | DECIMAL(10,2) | NOT NULL, CHECK >= 0 | 单价 |

### 6.3 可选扩展表

- **sales**：销售记录表，记录每次销售的 ISBN、数量、金额、销售时间，便于后续销售分析。

---

## 7. 界面设计

### 一、整体布局

系统采用 **左侧导航栏 + 右侧内容区** 的经典桌面应用布局。

```text
┌─────────────────────────────────────────────────────────────┐
│  顶部状态栏                                                  │
│  [系统标题]                              [当前用户/角色]      │
├───────────────┬─────────────────────────────────────────────┤
│               │                                             │
│   左侧导航栏   │            右侧主内容区                      │
│               │          （CardLayout 切换）                 │
│   • 数据概览   │                                             │
│   • 库存管理   │                                             │
│   • 图书销售   │                                             │
│   • 销售记录   │                                             │
│               │                                             │
└───────────────┴─────────────────────────────────────────────┘
```

#### 尺寸参考

| 元素 | 尺寸 |
|------|------|
| 窗口默认大小 | 1000 × 700 px |
| 左侧导航栏宽度 | 160 px |
| 顶部状态栏高度 | 约 45 px |
| 导航项高度 | 45 px |
| 面板内边距 | 15 px |

---

### 二、视觉风格

#### 当前配色

| 用途 | 颜色值 |
|------|--------|
| 顶部状态栏背景 | `#2980B9`（蓝色） |
| 导航栏选中项背景 | `#2980B9`（蓝色） |
| 导航栏选中项文字 | `#FFFFFF`（白色） |
| 导航栏未选中背景 | `#F5F5F5`（浅灰） |
| 导航栏未选中文字 | 默认黑色 |
| 导航栏边框 | `#DCDCDC`（浅灰） |
| 库存预警高亮背景 | `#FFC8C8`（浅红） |
| 库存预警文字 | `#FF0000`（红色） |

#### 字体

| 元素 | 字体 | 大小 | 字重 |
|------|------|------|------|
| 系统标题 | Microsoft YaHei | 18 px | Bold |
| 导航项 | Microsoft YaHei | 14 px | Plain / Bold（选中） |
| 表单标签 | 默认 | 12 px | Plain |
| 表格内容 | 默认 | 12 px | Plain |

#### 间距

- 面板内边距：`15px`
- 组件间距：`10px`
- 分组边框标题与内容间距：`5px`
- 按钮间距：`10px`

---

### 三、界面清单

#### 1. 登录对话框（LoginDialog）

```text
┌─────────────────────────┐
│       用户登录           │
├─────────────────────────┤
│  用户名 [____________]  │
│  密码   [____________]  │
│                         │
│    [登录]  [取消]       │
└─────────────────────────┘
```

**组成元素：**
- 标题：`用户登录`
- 输入框：用户名、密码
- 按钮：登录、取消
- 交互：密码框回车触发登录，错误时弹窗提示

---

#### 2. 数据概览面板（DataOverviewPanel）

```text
┌──────────────────────────────────────────────────────────┐
│ 数据概览                                                  │
├──────────────────────────────────────────────────────────┤
│ ┌─ 查询 ──────────────────────────────────────────────┐  │
│ │ 查询字段 [书名 ▼]  关键字 [________] [查询] [重置]  │  │
│ └───────────────────────────────────────────────────────┘  │
│ ┌─ 统计排序 ──────────────────────────────────────────┐  │
│ │ 排序字段 [价格 ▼]  方式 [降序 ▼]  [统计]            │  │
│ └───────────────────────────────────────────────────────┘  │
│  图书总数: XX   当前展示: XX                              │
│ ┌─────────────────────────────────────────────────────┐  │
│ │                                                     │  │
│ │          书籍列表表格（支持库存预警高亮）             │  │
│ │                                                     │  │
│ └─────────────────────────────────────────────────────┘  │
│ [导入 Excel/CSV]  [导出 Excel]                           │
└──────────────────────────────────────────────────────────┘
```

**组成元素：**
- 查询区：字段下拉框、关键字输入框、查询按钮、重置按钮
- 统计排序区：排序字段下拉框、排序方式下拉框、统计按钮
- 信息区：图书总数、当前展示数量
- 列表区：书籍表格（ISBN、书名、出版社、作者、库存、价格）
- 操作区：导入按钮、导出按钮

**表格列：**
| 列名 | 类型 |
|------|------|
| ISBN | 文本 |
| 书名 | 文本 |
| 出版社 | 文本 |
| 作者 | 文本 |
| 库存 | 整数 |
| 价格 | 金额 |

---

#### 3. 库存管理面板（InventoryPanel）

```text
┌──────────────────────────────────────────────────────────┐
│ 库存管理                                                  │
├──────────────────────────────────────────────────────────┤
│ ┌─ 搜索定位 ──────────────────────────────────────────┐  │
│ │ 搜索字段 [书名 ▼]  关键字 [________] [查询] [重置]  │  │
│ └───────────────────────────────────────────────────────┘  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │                                                     │  │
│ │          书籍列表表格（双击回填表单）                 │  │
│ │                                                     │  │
│ └─────────────────────────────────────────────────────┘  │
│ ┌─ 图书信息 ──────────────────────────────────────────┐  │
│ │  ISBN [________]  书名   [________]                │  │
│ │  出版社 [______]  作者   [________]                │  │
│ │  库存   [______]  价格   [________]                │  │
│ │                                                    │  │
│ │   [添加] [修改] [删除] [清空]                      │  │
│ └───────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

**组成元素：**
- 搜索区：字段下拉框、关键字输入框、查询按钮、重置按钮
- 列表区：书籍表格，支持双击回填
- 表单区：ISBN、书名、出版社、作者、库存、价格输入框
- 操作区：添加、修改、删除、清空按钮

---

#### 4. 图书销售面板（SalesPanel）

```text
┌──────────────────────────────────────┐
│ 图书销售                              │
├──────────────────────────────────────┤
│                                      │
│  ISBN:        [________________]    │
│  购买数量:    [________]            │
│                                      │
│  [查询]    [确认销售]                │
│                                      │
│  ┌─ 图书信息 ─────────────────────┐ │
│  │  书名:    -                   │ │
│  │  单价:    -                   │ │
│  │  库存:    -                   │ │
│  │  应收金额: -                  │ │
│  └────────────────────────────────┘ │
│                                      │
└──────────────────────────────────────┘
```

**组成元素：**
- 输入区：ISBN 输入框、购买数量输入框
- 操作区：查询按钮、确认销售按钮
- 信息区：书名、单价、库存、应收金额展示卡片

---

#### 5. 销售记录面板（SalesHistoryPanel）

```text
┌──────────────────────────────────────────────────────────┐
│ 销售记录                                                  │
├──────────────────────────────────────────────────────────┤
│ 销售笔数: XX   销售总额: XXX.XX   [刷新]                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │                                                     │  │
│ │          销售记录表格（按时间倒序）                   │  │
│ │                                                     │  │
│ └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

**组成元素：**
- 信息区：销售笔数、销售总额、刷新按钮
- 列表区：销售记录表格（ID、ISBN、书名、数量、金额、销售时间）

---

### 四、通用组件规范

#### 按钮

| 类型 | 建议颜色 | 用途 |
|------|---------|------|
| 主要操作 | `#2980B9` 蓝色 | 查询、添加、销售、登录 |
| 危险操作 | `#E74C3C` 红色 | 删除 |
| 次要操作 | `#95A5A6` 灰色 | 重置、清空、取消 |
| 成功操作 | `#27AE60` 绿色 | 导出、刷新 |

#### 输入框

- 高度：约 25 px
- 边框：1px 浅灰色
- 聚焦状态：蓝色边框
- 错误状态：红色边框（可扩展）

#### 表格

- 表头：浅灰背景 + 加粗文字
- 行高：约 25 px
- 选中行：系统默认蓝色
- 库存预警行：浅红背景 + 红色文字
- 交替行颜色：建议增加斑马纹提升可读性

#### 分组面板

- 使用 `TitledBorder` 包裹功能区域
- 标题字体加粗
- 内部留白 10~15px

---

### 五、状态定义

#### 导航项状态

| 状态 | 视觉表现 |
|------|---------|
| 未选中 | 浅灰背景，黑色常规文字 |
| 选中 | 蓝色背景，白色加粗文字 |
| 悬停（可选） | 略深的浅灰背景 |

#### 库存预警状态

| 阈值 | 视觉表现 |
|------|---------|
| 库存 ≥ 10 | 正常显示 |
| 库存 < 10 | 整行浅红背景 + 红色文字 |

#### 表单校验状态

| 状态 | 视觉表现 |
|------|---------|
| 正常 | 默认边框 |
| 错误 | 红色边框 + 错误提示弹窗 |
| 禁用 | 灰色背景，不可编辑 |

---

### 六、交互说明

| 交互 | 触发方式 | 效果 |
|------|---------|------|
| 切换功能模块 | 点击左侧导航项 | 右侧内容区切换，表单状态保留 |
| 查询图书 | 输入关键字点击查询 | 表格展示匹配结果 |
| 排序统计 | 选择字段和方式后点击统计 | 表格按条件排序展示 |
| 双击表格行 | 在库存管理表格双击 | 自动将图书信息回填到表单 |
| 销售查询 | 在销售页 ISBN 框回车或点击查询 | 显示图书信息和应收金额 |
| 导入导出 | 点击导入/导出按钮 | 弹出文件选择对话框 |
| 删除确认 | 点击删除按钮 | 弹出二次确认对话框 |

---

### 七、进一步美术设计建议

1. **统一主题色**
   - 当前主色为 `#2980B9`，可扩展一套配色：
     - 主色：`#2980B9`
     - 辅色：`#3498DB`
     - 深色：`#1F618D`
     - 浅色：`#EBF5FB`

2. **增加图标**
   - 导航项左侧增加小图标（数据概览 📊、库存管理 📦、图书销售 🛒、销售记录 📋、统计分析 📈）
   - 按钮可增加图标提升识别度

3. **现代化表格**
   - 表头使用主色背景 + 白色文字
   - 增加斑马纹
   - 行悬停高亮

4. **卡片化信息区**
   - 销售页的书籍信息区可设计成卡片样式
   - 销售记录页的统计数字可设计成 KPI 卡片

5. **圆角与阴影**
   - 按钮、输入框、卡片可增加轻微圆角（2~4px）
   - 内容区卡片可增加阴影提升层次感

6. **字体统一**
   - 建议全系统使用同一字体家族（如 Microsoft YaHei 或 PingFang SC）
   - 标题、正文、辅助文字形成明确层级

---

## 8. 验收标准

- [ ] Maven 项目可正常编译并运行。
- [ ] H2 数据库随程序启动，自动创建表结构。
- [ ] 支持图书的添加、查询、修改、删除功能。
- [ ] 添加和修改时 ISBN 重复能正确提示。
- [ ] 支持按书名、ISBN、作者、出版社查询。
- [ ] 销售功能正确计算金额并扣减库存。
- [ ] 库存不足时给出明确提示。
- [ ] 统计功能支持多字段排序。
- [ ] 程序重启后图书数据不丢失。
- [ ] 界面友好，关键操作有反馈，异常输入有校验。

---

## 9. 风险与注意事项

- H2 数据库文件默认生成在项目目录下，需在 `.gitignore` 中忽略 `*.mv.db`、`*.trace.db` 等文件。
- Swing 事件处理中直接访问数据库在简单场景下可接受，后续若数据量增大可考虑引入后台线程。
- 价格计算必须使用 `BigDecimal`，避免使用 `double`/`float`。
- 首次启动时可预置若干示例图书，便于功能演示。
