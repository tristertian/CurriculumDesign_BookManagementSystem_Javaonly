# 图书管理系统需求文档

## 1. 项目概述

### 1.1 项目背景

本项目为 Java 课程设计作业，目标是构建一个具备良好交互体验、数据持久化能力、且贴近实际生产实践的图书管理系统。

### 1.2 项目目标

实现一个基于 Java 的桌面图书管理系统，支持图书销售、图书信息管理、多条件查询、统计分析以及数据持久化，并通过图形化界面提升易用性。

### 1.3 适用范围

适用于单机环境下的图书销售与库存管理场景，可作为课程设计演示及后续扩展的基础版本。

---

## 2. 功能需求

### 2.1 图书销售

- 用户输入 ISBN 号，系统查询图书信息。
- 若图书存在且有库存，用户输入购买数量。
- 系统计算应收金额，扣减库存，并给出销售成功提示。
- 若库存不足，系统提示库存不足并结束本次销售。
- 应收金额计算精确到分，使用 `BigDecimal` 避免浮点误差。

### 2.2 图书信息管理

#### 2.2.1 添加图书

- 录入书名、出版社、ISBN、作者、库存量、价格。
- ISBN 号全局唯一，重复时提示并取消添加。
- 价格和库存量必须为非负数，必填项不能为空。

#### 2.2.2 查询图书

- 支持按书名、ISBN、作者、出版社进行查询。
- 书名、作者、出版社支持模糊查询。
- 查询结果在表格中展示。
- 无匹配记录时提示“该标题不存在！”。

#### 2.2.3 修改图书

- 根据查询结果选择记录进行修改。
- 修改时保持 ISBN 唯一性约束。
- 修改后刷新图书列表。

#### 2.2.4 删除图书

- 输入或选择要删除的 ISBN。
- 删除前进行二次确认。
- ISBN 不存在时提示“该编号不存在”。

### 2.3 统计功能

- 输出当前书库中图书总数及详细信息。
- 支持按价格、库存量、作者、出版社进行统计。
- 统计结果按指定字段从大到小排序展示。
- 支持排序字段切换及升序/降序调整。

### 2.4 数据持久化

- 使用 H2 嵌入式数据库存储图书数据。
- 程序启动时自动初始化数据库表结构。
- 程序运行期间的所有增删改查操作实时反映到数据库。
- 程序重启后数据不丢失。

---

## 3. 非功能需求

### 3.1 性能需求

- 单次查询响应时间不超过 1 秒（适用于万级以下图书数据）。
- 启动时间不超过 5 秒。

### 3.2 可靠性需求

- 数据库连接异常时给出友好提示，程序不崩溃。
- 输入非法数据时进行校验并给出明确错误信息。

### 3.3 易用性需求

- 采用图形化界面（Swing），降低操作门槛。
- 所有关键操作均有即时反馈。
- 表格支持排序、选中、滚动浏览。

### 3.4 可维护性需求

- 采用三层架构，表现层、业务逻辑层、数据访问层分离。
- 代码结构清晰，命名规范，关键逻辑添加注释。
- 使用 Maven 管理依赖，便于后续扩展。

---

## 4. 技术选型

| 技术/工具 | 选型 | 说明 |
|----------|------|------|
| 编程语言 | Java 17+ | 现代 Java 特性支持 |
| 构建工具 | Maven | 依赖管理与项目构建 |
| GUI 框架 | Swing | JDK 内置，无需额外运行环境 |
| 数据库 | H2 Database | 嵌入式数据库，零安装，随程序启动 |
| 数据库访问 | JDBC | 原生 SQL，便于学习和展示 |
| 金额计算 | BigDecimal | 避免浮点数精度问题 |

---

## 5. 系统架构

系统采用经典的三层架构：

```
┌─────────────────────────────────────┐
│  表现层（Presentation Layer）        │
│  Swing GUI：主窗口、图书管理面板、     │
│  销售面板、统计面板                   │
├─────────────────────────────────────┤
│  业务逻辑层（Service Layer）         │
│  BookService：销售、查询、修改、删除、 │
│  统计排序等业务规则                   │
├─────────────────────────────────────┤
│  数据访问层（Data Access Layer）     │
│  BookRepository / JdbcBookRepository │
│  负责 H2 数据库连接与 CRUD 操作       │
├─────────────────────────────────────┤
│  实体层（Entity Layer）              │
│  Book：图书数据模型                   │
└─────────────────────────────────────┘
```

---

## 6. 数据库设计

### 6.1 图书表（books）

```sql
CREATE TABLE IF NOT EXISTS books (
    isbn VARCHAR(20) PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    publisher VARCHAR(100),
    author VARCHAR(100),
    stock INT NOT NULL CHECK (stock >= 0),
    price DECIMAL(10, 2) NOT NULL CHECK (price >= 0)
);
```

### 6.2 字段说明

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| isbn | VARCHAR(20) | PRIMARY KEY | 国际标准书号，唯一标识 |
| title | VARCHAR(100) | NOT NULL | 书名 |
| publisher | VARCHAR(100) | | 出版社 |
| author | VARCHAR(100) | | 作者 |
| stock | INT | NOT NULL, CHECK >= 0 | 库存量 |
| price | DECIMAL(10,2) | NOT NULL, CHECK >= 0 | 单价 |

### 6.3 可选扩展表

- **sales**：销售记录表，记录每次销售的 ISBN、数量、金额、销售时间，便于后续销售分析。

---

## 7. 界面设计

### 7.1 登录界面
- 输入用户名以及密码
- 选择登录或者取消


### 7.2 管理员主窗口

- 窗口标题：图书管理系统-管理员
- 布局：纵向标签页切换不同功能模块

### 7.2 图书管理标签页

- 上方：搜索区域（下拉选择查询字段 + 输入框 + 查询按钮）
- 中部：图书列表表格
- 下方：图书信息录入/编辑表单（ISBN、书名、出版社、作者、库存、价格）
- 操作按钮：添加、修改、删除、清空

### 7.3 销售标签页

- 输入区域：ISBN、购买数量
- 显示区域：图书名称、单价、库存、应收金额
- 操作按钮：查询、确认销售

### 7.4 统计标签页

- 显示图书总数
- 排序选择：价格、库存量、作者、出版社
- 排序方式：升序 / 降序
- 结果表格展示

---

## 8. 验收标准

- [ ] Maven 项目可正常编译并运行。
- [ ] H2 数据库随程序启动，自动创建表结构。
- [ ] 支持图书的添加、查询、修改、删除功能。
- [ ] 添加和修改时 ISBN 重复能正确提示。
- [ ] 支持按书名、ISBN、作者、出版社查询。
- [ ] 销售功能正确计算金额并扣减库存。
- [ ] 库存不足时给出明确提示。
- [ ] 统计功能支持多字段排序。
- [ ] 程序重启后图书数据不丢失。
- [ ] 界面友好，关键操作有反馈，异常输入有校验。

---

## 9. 风险与注意事项

- H2 数据库文件默认生成在项目目录下，需在 `.gitignore` 中忽略 `*.mv.db`、`*.trace.db` 等文件。
- Swing 事件处理中直接访问数据库在简单场景下可接受，后续若数据量增大可考虑引入后台线程。
- 价格计算必须使用 `BigDecimal`，避免使用 `double`/`float`。
- 首次启动时可预置若干示例图书，便于功能演示。
